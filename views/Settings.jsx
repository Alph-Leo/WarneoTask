import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

 const Settings = () => {
    <SafeAreaView>
        <View><Text>Nothing to set yet</Text></View>
    </SafeAreaView>
 };
 
 export default Settings;